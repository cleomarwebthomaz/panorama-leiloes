@layout('admin.layouts.master')

@section('title')
  Adicionar novo tipo de Contato
@endsection

@section('breadcrumb')
  {{ addCrumb('Configurações', route('admin.contact-type.index')) }}
  {{ addCrumb('Adicionar') }}
@endsection

@section('content')
<div class="card card-outline card-primary">
  <div class="card-header">
    <h3 class="card-title">Tipo de Contatos</h3>
  </div>
  <!-- /.card-header -->

  <!-- form start -->
  <form role="form" action="{{ route('admin.contact-type.store') }}" method="POST">
    {{ csrfField() }}

    <div class="card-body">
        @include('admin.pages.contact-type._partials.form')
    </div>

    <div class="card-footer">
      <button type="submit" class="btn btn-primary"><i class="fa fa-save"></i> Adicionar</button>
    </div>
  </form>
</div>
@endsection
