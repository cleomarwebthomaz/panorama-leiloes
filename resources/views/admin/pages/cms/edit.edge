@layout('admin.layouts.master')

@section('title')
  Editar Página: <small>{{ page.title }}</small>
@endsection

@section('breadcrumb')
  {{ addCrumb('Páginas', route('admin.page.index')) }}
  {{ addCrumb('Editar') }}
@endsection

@section('content')

@if(flashMessage('success'))
  <div class="alert alert-success">{{ flashMessage('success') }}</div>
@endif

<div class="card card-outline card-primary">
    <div class="card-header">
      <h3 class="card-title">Páginas</h3>
    </div>
    <!-- /.card-header -->
    <!-- form start -->
    <form role="form" action="{{ route('admin.page.update', {id: page.id}) }}?_method=PATCH" method="POST" enctype="multipart/form-data">
      {{ csrfField() }}

      <div class="card-body pad">
          @include('admin.pages.cms._partials.form')
      </div>
      <!-- /.card-body -->

      <div class="card-footer">
        <button type="submit" class="btn btn-primary"><i class="fa fa-save"></i> Atualizar</button>
      </div>
    </form>
  </div>
@endsection
